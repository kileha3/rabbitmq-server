#auth_oauth2.oauth_provider_id = fakeportal
#auth_oauth2.oauth_providers.fakeportal.issuer = ${OAUTH_PROVIDER_URL}
#auth_oauth2.oauth_providers.fakeportal.ssl_options.verify = verify_peer
management.oauth_provider_url = ${FAKEPORTAL_URL}
